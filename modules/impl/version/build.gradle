dependencies {
    implementation libs.jitsi.basesvc
    implementation libs.commons.lang
    implementation project(':util')
    implementation project(':osgi-util')
}

processResources {
    filter(org.apache.tools.ant.filters.ReplaceTokens, tokens: [
            versionName      : project.getProperty('application.name').toString(),
            versionMajor     : versionMajor.toString(),
            versionMinor     : versionMinor.toString(),
            versionBuildId   : "${versionRevision}+${versionHash}".toString(),
            versionPrerelease: versionSuffix.toString(),
    ])
}